package org.citruscircuits.vision.effects;

import org.opencv.core.Mat;
import org.opencv.core.Scalar;

public abstract class IteratePixelEffect extends ImageEffect {
	@Override
	public Mat process(Mat original) {
		Mat altered = Mat.zeros(original.size(), original.type());
		for (int row = 0; row < original.rows(); row++ ) {
	        for(int col = 0; col < original.cols(); col++ ) {
	            altered.put(row, col, processPixel(original.get(row, col), row, col));
	        }
	    }
		original.release();
		return altered;
	}
	
	public abstract double[] processPixel(double[] in, int row, int col); // Called once for every pixel
}
